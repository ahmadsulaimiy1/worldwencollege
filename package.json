{
  "name": "wec-lc",
  "private": true,
  "description": "WorldWide English College, London Campus — public site (static, no framework) + backend (Cloudflare Pages Functions, no npm runtime dependencies).",
  "comment_on_modules": "No root-level \"type\" field, deliberately: scripts/build.js (the existing static-site generator) is CommonJS, while functions/**/*.js (Pages Functions) use ES module import/export. Node 22's built-in CJS/ESM syntax auto-detection handles both correctly from one tree without a type field — verified by running both. Do not add \"type\": \"module\" here without also converting scripts/build.js, or it breaks the static build.",
  "comment_on_engines": "scripts/build.js alone would run on much older Node, but `npm test` needs 22+ for node:sqlite (see tests/README.md) — the honest minimum for full local development, not just the static build.",
  "scripts": {
    "build": "node scripts/build.js",
    "faculty": "node scripts/build-faculty.js && node scripts/build.js",
    "arabic": "node scripts/build-arabic.js && node scripts/build-arabic-levels.js && node scripts/build.js",
    "test": "node --experimental-sqlite tests/run.mjs",
    "dev": "wrangler pages dev . --d1=DB",
    "db:create": "wrangler d1 create wec-lc",
    "db:schema": "wrangler d1 execute wec-lc --remote --file=sql/schema.sql",
    "db:schema:local": "wrangler d1 execute wec-lc --local --file=sql/schema.sql",
    "deploy": "wrangler pages deploy .",
    "publication": "node --experimental-sqlite scripts/publication/render-docx.mjs && node --experimental-sqlite scripts/publication/render-pdf.mjs",
    "curriculum": "node --experimental-sqlite scripts/publication/render-flagship.mjs && node --experimental-sqlite scripts/publication/render-curriculum-docx.mjs && node --experimental-sqlite scripts/publication/render-specs.mjs && node --experimental-sqlite scripts/publication/render-bible.mjs",
    "curriculum:large": "IEFC_TYPE_SCALE=1.35 node --experimental-sqlite scripts/publication/render-flagship.mjs",
    "curriculum:student": "IEFC_EDITION=student node --experimental-sqlite scripts/publication/render-flagship.mjs",
    "curriculum:institutional": "IEFC_EDITION=institutional node --experimental-sqlite scripts/publication/render-flagship.mjs",
    "curriculum:editions": "npm run curriculum:student && npm run curriculum:institutional",
    "press": "node --experimental-sqlite scripts/publication/render-press.mjs && node --experimental-sqlite scripts/publication/render-press.mjs",
    "pronunciation": "node --experimental-sqlite scripts/publication/render-pronunciation.mjs",
    "canon": "node --experimental-sqlite scripts/publication/render-canon.mjs && node --experimental-sqlite scripts/publication/render-canon.mjs",
    "listening": "node --experimental-sqlite scripts/publication/render-listening.mjs",
    "assessment": "node --experimental-sqlite scripts/publication/render-assessment.mjs",
    "workbook": "node --experimental-sqlite scripts/publication/render-workbook.mjs",
    "companion": "node --experimental-sqlite scripts/publication/render-companion.mjs"
  },
  "devDependencies": {
    "@fontsource/amiri": "^5.3.0",
    "@fontsource/cairo": "^5.3.0",
    "@fontsource/inter": "^5.3.0",
    "@fontsource/playfair-display": "^5.3.0",
    "jsqr": "^1.4.0",
    "pdfjs-dist": "4.6.82",
    "playwright": "^1.62.1",
    "wrangler": "^3"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "docx": "^9.7.1"
  }
}
