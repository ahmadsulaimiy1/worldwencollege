{
  "name": "@stromex/design-system",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "The StromeX Design Language, implemented. Tokens, the Meridian, the Quire, Stratum, the Chronograph, the Register and the signature components \u2014 with the gates that check them.",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "fonts": "node build/fonts.mjs",
    "art": "node build/generate-guilloche.mjs",
    "build": "npm run fonts && npm run art",
    "gate:tokens": "node gates/tokens.mjs",
    "gate:beat": "node gates/beat.mjs",
    "gate:contrast": "node gates/contrast.mjs",
    "gate:responsive": "node gates/responsive.mjs",
    "gates": "npm run gate:tokens && npm run gate:beat && npm run gate:contrast && npm run gate:responsive",
    "unit": "node --test \"test/*.test.mjs\"",
    "test": "npm run gates && npm run unit"
  },
  "dependencies": {
    "@fontsource-variable/fraunces": "^5.3.0",
    "@fontsource-variable/archivo": "^5.3.0",
    "@fontsource-variable/cinzel": "^5.3.0"
  },
  "optionalDependencies": {
    "playwright": "^1.49.0"
  }
}
